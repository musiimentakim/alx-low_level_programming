#include <stdio.h>
/**
 * main - Function
 * @argc: parameter count
 * @argv: parameter array
 * Return: Always 0 for success and any positive integer otherwise
 */

int main(int argc, char *argv[])
{
	argv = argv;

	printf("%d\n", argc - 1);
	return (0);
}

